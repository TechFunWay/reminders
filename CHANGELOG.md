# 更新日志

本文件记录“提醒事项”的新功能、问题修复和使用体验变化。

## v0.3.0（2026-09-19）

本版本包含自 v0.2.0 以来的全部功能与体验更新。

### 新增功能

- 电子邮件通知支持绑定多个接收邮箱：绑定第一个邮箱后可继续添加，到点的提醒会同时发送给全部已启用的接收邮箱；每个邮箱可单独删除，也可用渠道开关统一启停，家人或工作邮箱都能一起接收重要提醒。
- 每条提醒可单独指定发送到哪些接收邮箱：编辑提醒时勾选即可（不选则发送给全部），例如工作事项只发工作邮箱；所选邮箱被解绑后，提醒会自动改发给其余已启用的邮箱，不会漏发。
- 新增“再次提醒”：到点后如果没有点击完成，提醒会按设定的间隔反复通知，直到点击完成、编辑或删除该提醒为止；间隔支持 5 分钟到 31 天自定义。配合“每天”重复规则，一个提醒即可实现“每天固定时间催我”。
- 新增农历重复提醒：支持“每年（农历）”和“每月（农历）”循环，适合农历生日与农历纪念日；腊月三十遇小年自动落在除夕当天，闰月日期遇无闰月年份自动用对应平月，点击完成后自动排到下一个农历周期。
- 日期选择升级为公历/农历双页签日历：公历格子直接标注农历日名，农历页签按“初一、廿一”等传统叫法展示并标注对应公历日期，节日红字、节气绿字，一眼找到农历日期。
- **跨设备自动同步。** 此前实时通道只在“站内通知”产生时推一条消息，提醒自身的增删改一条事件都不发，所以在手机端加了提醒，电脑端要手动刷新才看得到。现在任何数据变更（新建 / 修改 / 完成 / 取消完成 / 稍后提醒 / 删除提醒、清单的增删改、通知标记已读、通知渠道绑定变化）都会立刻广播给同一账号的其它设备，对方自动刷新列表、侧栏计数与通知中心——手机改了电脑跟着变，电脑改了手机跟着变。
  - 每个浏览器标签页有独立标识，广播时跳过发起变更的那一个（它自己已经更新过界面）；所以同一台设备开两个窗口也会互相同步。
  - 另有一道兜底：前端每 20 秒（以及从后台切回前台时）核对一次服务端的变更版本号，变了就重新拉数据。实时长连接要穿过飞牛统一网关那层代理，万一响应被缓冲，靠这道兜底也能保证改动最终一定同步过来。
- **站内提醒改为屏幕中央的弹窗。** 提醒到点时以前只在右下角飘一条 toast，手机端那个位置既容易被忽略、又容易被底部标签栏挡住，等于没提醒。现在改为屏幕中央的弹窗，提供「完成」「稍后提醒」（10 分钟）「知道了」三个操作，电脑与手机一致；多个提醒同时到点会排队逐条处理，并显示还有几条待处理，到点提示音保留。其它类型的通知（例如渠道投递失败）仍保持轻量的 toast，不打断操作。

### 飞牛 fnOS

- **桌面主入口固定在飞牛统一网关域**：桌面图标、飞牛手机 App、飞牛远程域名三种入口下，应用页面都跑在网关域（`/app/techfunway-reminders/`），飞牛会话与页面同源，登录票据当场签发、就地确认，**全程零跳转零弹窗**。此前入口一度放在应用自己的直连端口上，直连端口签不到登录票据，只能整页跳到网关域取票，而飞牛 App 的内嵌网页视图会拦下这次跨端口跳转，用户就被丢回登录页（电脑浏览器切手机模式能成功，所以这个原因一直没暴露）。局域网直连端口访问、旧书签仍可用隐藏的登录跳板页取票。
- **用飞牛 NAS 登录后可以独立退出本应用。** 飞牛授权只是**身份来源**，和 QQ / 微信登录第三方应用是同一回事：授权方还登录着，不代表应用自己的会话不能结束。此前把两者绑成了一个东西——网关域上服务端每个请求都拿网关注入的 NAS 身份重新认人，本地登出会被下一个请求立刻撤销，界面只好把「退出登录」按钮整个隐藏，偏好设置里只留一句“请在飞牛桌面退出登录”，用户因此完全退不出应用。现在点「退出登录」会先让服务端记下这次**主动登出**，此后网关注入身份不再自动换回登录态，退出即时生效，刷新页面、重开应用都保持退出；「退出登录」按钮在顶栏与偏好设置始终可见。
  - 退出的是本应用、不是飞牛账号：想回来时在登录页点一次“使用飞牛 NAS 登录”即可重新进入，不用重新输入应用账号密码。
  - 反向逻辑保留：只有登录态完全由飞牛身份维持时（用户没在本应用里显式登录过），在飞牛桌面或飞牛 App 退出登录后，本应用才跟着回到登录页；应用自己签发的会话不被连带踢下线。
- **网关域上刷新不再掉登录态。** 根因是飞牛接入层会把请求里的 `Authorization` 当成**它自己的**会话 token：应用带着自己的登录凭证请求时，接入层认不出来，直接回一段纯文本 `invalid token`，请求根本到不了应用——于是登录之后每个需鉴权接口都失败（列表空、配置加载失败），刷新时启动引导读到“未登录”就清掉会话。现在服务端在可信网关连接上支持用网关注入的 `X-Trim-Userid` 解析登录态（映射到该 NAS 用户已绑定的应用账号），前端在网关域不再发送自己的 `Authorization`；网关域打开应用即是已登录状态，直连端口的登录方式完全不受影响，未绑定的 NAS 用户拿不到任何身份。
- **飞牛授权按环境自动分档，并有两道守卫**：桌面端打开居中的授权弹窗（弹窗被拦截、或 2.5 秒内没有报到就自动降级），手机端与飞牛 App 网页视图直接在应用内弹确认框（手机网页视图会忽略关窗，独立窗口会把用户晾在“授权成功，可关闭本窗口”而看不到后续注册 / 绑定页），直连端口整页走跳板页取票。票据只经 postMessage 或同标签页跳转的 URL hash 回传，不落 sessionStorage。
- 支持在授权流程内切换其他飞牛账号：弹窗内打开飞牛登录页，`redirect_uri` 指回同源授权页（飞牛登录页只接受同源回跳）。手机端不再显示无法可靠完成的换号入口，改为提示在飞牛 App 中切换后重新打开。
- 修复从外网（远程域名 / 飞牛 App）登录或换号时跳到**内网地址**导致“无法访问页面”的问题：应用自身跑在网关域时一律优先使用当前访问地址（这是唯一一定可达的入口），陈旧的内网登记只作直连端口场景的兜底，并且每次打开登录 / 注册页都会重新解析一次、就地纠正缓存里的旧值。
- 首次安装默认走“用户名密码创建管理员”，不默认进入飞牛授权；想用飞牛一键登录时主动点击“使用飞牛 NAS 登录”并确认后才进入绑定创建，注册页内即可确认飞牛账号、自动预填用户名。已有用户时进入登录并绑定流程，避免误创建没有原有提醒数据的空白账号。
- 应用页面响应头禁用缓存，升级后浏览器立即使用新版前端；网关路径不带末尾斜杠时会自动回到首页，不再渲染空白页。

### 问题修复

- **修复提醒到点后没有弹窗提示的问题。** 到点弹窗此前只由实时长连接的推送事件触发，而这条长连接要穿过飞牛统一网关的代理：响应一旦被缓冲、手机切到后台后连接被系统掐断、或服务端重启，事件就丢了——通知明明记进了通知中心，屏幕上却什么都不会出现。现在前端会定期核对服务端的变更版本号，发现错过事件就主动拉取未读的到点通知补进弹窗队列；刚打开应用时，最近一小时内到点且还没处理的提醒也会补弹。弹窗上点过「完成 / 稍后提醒 / 知道了」的通知都已标为已读，不会重复弹。
- 从列表里直接完成、稍后提醒或删除一条提醒时，它那条“到点了”的未读通知现在会一并标为已读（与提醒的改动在同一个数据库事务里）：通知中心不再残留已处理提醒的未读角标，其它设备上也不会再把它弹出来。
- 非飞牛环境（裸二进制 / Docker 部署）的登录与注册页不再显示「使用飞牛 NAS 登录」按钮：按钮显隐从编译期标志改为跟随服务端实际运行形态（随启动引导下发），同一套飞牛版前端被非飞牛部署直接运行时不会再出现这个点了必然失败的入口。
- 修复登录成功之后整个应用不可用的问题（刷新即掉登录态、添加的提醒看不到、通知渠道不显示、偏好设置提示加载配置失败、用户管理与系统配置无数据），根因见上面“网关域上刷新不再掉登录态”。
- 修复“今天”页添加不带日期的提醒后列表里看不到、像是没保存的问题：这类提醒不属于按到期时间过滤的视图，现在会切到“全部”并提示已保存到哪里。
- 修复界面把“加载失败”伪装成“这里很安静 / 暂无用户数据”的问题：通知中心与用户管理都会区分“真的空”与“加载失败”，失败时给出原因和重试入口。
- 修复在缺少系统时区库的精简环境（飞牛根文件系统、最小化容器镜像）里提醒列表与汇总接口直接报错的问题：IANA 时区库编进二进制，不再依赖宿主机是否安装 tzdata。
- 修复与同域兄弟应用（如彩彩助手）互相顶掉登录态的问题：此前双方都把登录令牌写进同一个 `token` 键，一边登录另一边的登录态即被覆盖；现在本地存储键全面加应用前缀，并且只接受指向本应用跳板页的入口地址。
- 修复电脑端授权弹窗没有居中、偏到屏幕一侧的问题：弹窗坐标改用屏幕可用区计算，多显示器与高 DPI 缩放下都居中。
- 其它修复：“再次提醒”只响一次之后不再催办、从日历选择的日期提交报“提醒参数无效”、农历日历弹层在电脑端被快速添加卡片裁剪、标签页标题先显示英文再变中文、手机端经飞牛远程地址打开是空白页、电脑端首次安装后飞牛已提示打开却要等约十秒才能访问、经网关访问时所有 NAS 用户共用同一限流额度相互顶成 429、首次安装初始化卡住时浏览器无限白屏、通知渠道图标出现空图标、历史数据缺渠道字段导致整行渲染中断、用户管理统计卡片口径混用（本页与全局区分）。

### 体验优化

- 删除提醒、删除用户、退出登录的原生确认弹窗全部改为应用内确认框：原生弹窗在飞牛 App 的网页视图里样式与应用割裂，手机端也容易被当成浏览器提示忽略；确认框默认把焦点放在“取消”上（避免一次回车就删掉数据），支持 `Esc` 与点击遮罩取消，危险操作的失败原因显示在浮层内。
- 首屏不再长时间白屏：页面解析完即显示应用图标与“正在加载”骨架，入口包下载、启动请求与路由判定期间都有画面；启动请求从两到三次往返合并为一次，弱网下首屏少等两跳；启动等待上限从 8 秒收紧到 4 秒，服务未就绪时给出明确提示并可重试。
- 前端资源与接口响应统一启用 gzip 压缩（入口包 160KB 压到约 62KB、样式 66KB 压到约 11KB，手机端经飞牛网关远程访问时提升最明显）；带内容哈希的构建产物改为长期强缓存，图标类静态文件缓存一周，应用入口页仍然禁用缓存；去掉首页多余的 512 像素图标声明（添加到主屏才需要，此前每次打开都白下约 300KB）。
- 手机端全面紧凑化贴近原生观感：顶栏收窄到 48px，卡片、表单、列表行距统一收紧，一屏可见内容显著增加；滚动时顶栏与底部标签栏不透明；日期选择改为底部抽屉、时间选择用时 / 分滚轮、日历支持左右轻扫翻月；键盘弹出时自动隐藏底部标签栏，不再盖住输入框；安卓系统“大字体”缩放不再影响网页布局。
- 日期选择器打开时默认选中今天，单独修改时间即时生效，选中后按钮显示完整“年月日 时分”。
- 安全提示的“稍后再说”时间戳改用带应用前缀的存储键，避免飞牛网关同源下与兄弟应用互相覆盖。

## v0.2.0（2026-08-17）

本版本包含自 v0.1.4 以来的重要功能与体验更新。

### 新增功能

- 新增钉钉个人群自定义机器人通知。无需创建钉钉应用，粘贴机器人 Webhook 即可绑定、测试并接收提醒，地址会加密保存。
- 新增飞牛 fnOS 一键登录和应用账号绑定。飞牛用户可以创建首个管理员、绑定已有应用账号，并在电脑端和移动端继续使用同一份提醒、清单及通知方式数据。
- 新增飞牛登录授权确认窗口。只有用户确认后才会登录；支持进入飞牛系统登录页切换其他 NAS 账号，返回后重新确认。

### 飞牛 fnOS

- 飞牛桌面应用改为新页面打开，不再使用小窗口形式。
- 修复通过飞牛远程链接在手机端无法打开应用的问题。
- 修复电脑端和手机端可能进入不同应用账号，导致提醒及钉钉配置不同步的问题。
- 首次安装且没有应用用户时，直接进入管理员创建页面；已有用户时进入登录并绑定流程，避免误创建没有原有提醒数据的空白账号。
- 完善飞牛登录页、注册页和账号绑定说明，并优化手机端显示效果。

### 使用体验

- 修复 Docker 镜像启动时提示找不到 `reminder` 程序、导致容器创建失败的问题。
- 提升低配 NAS、手机端和长提醒列表的运行流畅度。
- 减少切换视图、新增、修改、完成或删除提醒时不必要的整页刷新，操作响应更快。
- 优化 QQ 机器人断线后的自动恢复，减少重复错误提示。
- 完善钉钉群机器人配置步骤和接收范围说明。

## v0.1.4（2026-08-04）

- 支持在飞牛应用安装、升级和设置时配置服务端口。
